from enum import Enum

class PrimitiveVocabulary(Enum):
    BIN = "Bin"
    CAST = "Cast"
    ENUM = "EnumToEnum"
    ROUND = "Round"
    THRESHOLD = "Threshold"
    TRUNCATE = "Truncate"
    UNITS = "ConvertUnits"
    REDUCE = "Reduce"
    NOTHING = "DoNothing"
